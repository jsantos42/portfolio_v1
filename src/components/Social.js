import styled from "styled-components";

const Social = styled.div`
  display: flex;
  flex-direction: row;
  gap: 1.5rem;
  padding: 0 0 1rem 0;
  & img:hover {
    opacity: 50%;
  }
`

export default Social;